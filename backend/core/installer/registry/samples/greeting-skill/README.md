# Greeting Skill

A minimal sample skill for testing the CodeCraft Skill Installer. Adds one
tool, `greeting.sayHello`, which returns a friendly message.

Install it to verify the installer pipeline end to end:

```
cc install greeting-skill
```
